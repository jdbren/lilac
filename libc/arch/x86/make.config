ARCH_CFLAGS=
KERNEL_ARCH_CFLAGS=-mno-red-zone -mgeneral-regs-only

ARCH_FREEOBJS=\

ARCH_HOSTEDOBJS=\

ifdef CONFIG_x86_64-elf
ARCH_CFLAGS+=-DARCH_x86_64
KERNEL_ARCH_CFLAGS+=-mcmodel=kernel -DARCH_x86_64
endif
